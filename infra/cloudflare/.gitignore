# Backend source copied in by deploy-cloudflare.sh — backend/ is the source of truth.
/services/
/prompts/

# Build artifacts from pywrangler sync / uv (never committed, never uploaded to CF)
/.venv/
/.venv-workers/
/python_modules/
/pylock.toml
/uv.lock
/requirements.txt
/node_modules/
.wrangler/
